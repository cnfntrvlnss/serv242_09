CC=gcc
CCC=g++
CXX=g++

CND_PLATFORM=Intel-Linux-x86
CONF=Debug
CND_DISTDIR=dist09
CND_BUILDDIR=build09
OBJDIR=${CND_BUILDDIR}/${CONF}/${CND_PLATFORM}/
DISTDIR := ${CND_DISTDIR}/${CONF}/${CND_PLATFORM}/
MODE=Test
TESTDIR=../test/ioacases/

VPATH=../src/:../src/audiz/

CPPNAMES:= audizserv_p.cpp BufferProject_fork.cpp
CPPNAMES +=
CPPNAMES +=
CPPNAMES+=
CNAMES:= apueclient.c
PROGNAME=recMain
LDLIBS +=
LDFLAGS += -lpthread 
CFLAGS +=
ifneq (${CONF},Debug)
	CFLAGS+= -O2 -Wall
else
	CFLAGS+= -g -Wall
endif
